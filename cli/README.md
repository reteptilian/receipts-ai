# Receipts AI CLI

A small Textual-based command line interface for Receipts AI.

## Development

Install dependencies:

```sh
uv sync
```

Run the CLI:

```sh
uv run receipts-ai-cli
```

Run checks:

```sh
uv run ruff check .
uv run basedpyright
uv run pytest
```
